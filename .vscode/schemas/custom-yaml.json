{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "JSON schema for YAML files in @rtapp-verify/lists",
  "description": "JSON schema implementation for YAML files in lists/**/ directories."
}
